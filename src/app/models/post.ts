export interface PostInterface {
    id?: string,
    title?: string,
    description?: string,
    tags?: any,
    img?: any,
    tools?: any,
    // content?: string,
    // download?: string,
    date?: string,
    // captures?: Array<any>
}
